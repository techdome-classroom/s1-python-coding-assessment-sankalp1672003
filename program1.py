class Solution:
    def dfs(r,c):
        if 0<=r
    def getTotalIsles(self, grid: list[list[str]]) -> int:
    #    write your code here
        if not grid:
            return 0
        r,c=len(grid),len(grid[0])
        island=0
                   
        return 0
